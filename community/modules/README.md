# Community Modules

Hardware modules contributed by the community.
Not maintained by the core project.

Each module must declare its status: `experimental`, `tested`, or `stable`.
See `spec/module-interface.md` for interface requirements.
